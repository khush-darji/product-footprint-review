# Base URL of the backend API.
#
# NEXT_PUBLIC_* variables are inlined into the client bundle at BUILD time, not read at
# run time — which is why docker-compose passes this to the frontend image as a build
# arg. It must be a URL the user's browser can reach, not a Docker service name.
NEXT_PUBLIC_API_URL=http://localhost:4000
